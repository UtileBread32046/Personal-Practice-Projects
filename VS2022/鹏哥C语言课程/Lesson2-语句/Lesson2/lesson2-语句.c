#include <stdio.h>
/*阿巴阿巴*/ // 此种注释不支持嵌套但可插入与分行
// 注释时加上→ \
   可作为续行符为下一行作注释
int main()
// {}中的代码构成复合语句
{ 
	; // 空语句
	int a = 20; // int:定义整型变量并赋值
	int b; // 函数调用语句
	b = a + 5; // 表达式语句(必须有;)
	printf("%d\n", b); // 函数调用语句
	return 0;
}