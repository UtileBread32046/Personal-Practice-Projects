#include <iostream>
#include <math.h>
using namespace std;

int main()
{
  / 常用数学函数
  * pow(power):乘方(谁的多少次方)
  * sqrt(squae root):开平方
  * cbrt(cube root):开立方
  * abs(absolute):绝对值
  * fomd(float modulo):浮点取模
  * ceil(ceiling):向上取整
  * floor:向下取整
  * round:四舍五入
  * hypot(hypotenuse):勾股定理求斜边
  * sin, cos, tan, asin, acos, atan:三角函数&反三角函数
  * log, log10:对数函数(此处的log即为ln)
  */

  return 0;
}